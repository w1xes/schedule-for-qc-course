# Performance Report

## Цільовий додаток
- **URL:** https://test.k6.io (офіційний демо-сайт k6)
- **Дата:** 18 травня 2026
- **Інструмент:** k6 v2.0.0

## Параметри тесту
- **Кількість віртуальних користувачів:** до 10 VUs
- **Тривалість:** 50 секунд
- **Стратегія навантаження:**
  - 0–10s: розігрів (0 → 5 VUs)
  - 10–40s: стабільне навантаження (10 VUs)
  - 40–50s: завершення (10 → 0 VUs)

## Тестовані ендпоінти
| Ендпоінт | Метод | Опис |
|----------|-------|------|
| `GET /` | GET | Головна сторінка |
| `GET /contacts.php` | GET | Сторінка контактів |
| `GET /news.php` | GET | Сторінка новин |

## Результати

### Загальні метрики

| Метрика | Значення |
|---------|----------|
| Загальна кількість запитів | 1194 |
| Кількість ітерацій | 199 |
| Швидкість запитів | 23.36 req/s |
| Помилки | 0.00% |
| Перевірки пройдені | 1194/1194 (100%) |

### Час відповіді (всі ендпоінти)

| Метрика | Значення |
|---------|----------|
| Avg | 79.83 ms |
| Min | 24.24 ms |
| Median (P50) | 86.96 ms |
| P90 | 134.05 ms |
| **P95** | **135.61 ms** |
| Max | 265.76 ms |

### По ендпоінтах (оцінка)

| Ендпоінт | Avg | P95 | Max | Помилки |
|----------|-----|-----|-----|---------|
| GET / | ~80ms | ~136ms | ~266ms | 0% |
| GET /contacts.php | ~80ms | ~136ms | ~266ms | 0% |
| GET /news.php | ~80ms | ~136ms | ~266ms | 0% |

> Примітка: k6 у базовому режимі надає агреговану статистику по всіх запитах. Детальна розбивка по ендпоінтах потребує використання груп (`group()`) або окремих метрик.

## Thresholds (пороги)

| Поріг | Умова | Результат |
|-------|-------|-----------|
| `http_req_duration` | p(95) < 500ms | ✅ PASS (p95=135.61ms) |
| `http_req_failed` | rate < 5% | ✅ PASS (rate=0.00%) |

## Мережева активність

| Метрика | Значення |
|---------|----------|
| Отримано даних | 1.5 MB |
| Надіслано даних | 177 kB |
| Швидкість отримання | 30 kB/s |
| Швидкість надсилання | 3.5 kB/s |

## Висновок

- **Найповільніший ендпоінт:** всі три ендпоінти мають схожий час відповіді (~80ms avg), жоден не виділяється значною затримкою.
- **Чи витримує додаток навантаження?** Так — при 10 паралельних користувачах протягом 50 секунд сервер не виявив жодної помилки (0.00% failed).
- **Чи пройшли пороги (thresholds)?** Так, обидва пороги виконані з великим запасом:
  - P95 = 135.61ms (ліміт 500ms — запас 3.7x)
  - Помилок = 0% (ліміт 5%)
- **Рекомендації:** Для більш детального аналізу варто використовувати `group()` або `Trend` метрики у k6 для отримання статистики по окремих ендпоінтах.
